[project]
name = "vorarbeiter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.7",
    "dj-database-url>=2.2.0",
    "gunicorn>=21.2.0",
]
